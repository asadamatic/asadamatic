import 'package:flutter/material.dart';

class ChatStatus extends StatelessWidget {
  const ChatStatus({Key? key, this.status}) : super(key: key);

  final String? status;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
